/**
* DevExpress Analytics (serializer\disposable.js)
* Version:  25.2.4
* Build date: Jan 30, 2026
* Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
* License: https://www.devexpress.com/Support/EULAs/universal.xml
*/
export class Disposable {
    constructor() {
        this._disposables = [];
        this.isDisposing = false;
    }
    disposeObservableArray(array) {
        if (array) {
            array().forEach((item) => { item.dispose && item.dispose(); });
        }
    }
    resetObservableArray(array) {
        if (array)
            array([]);
    }
    disposeArray(array) {
        if (array) {
            array.forEach((item) => { item.dispose && item.dispose(); });
            array.splice(0, array.length);
        }
    }
    addDisposable(...disposables) {
        this._disposables.push(...disposables.map(x => {
            if (!x)
                return x;
            if ('dispose' in x)
                return x;
            else {
                return { dispose: x };
            }
        }));
    }
    dispose() {
        if (!this.isDisposing) {
            this.isDisposing = true;
            (this._disposables || []).reverse().forEach(x => x && x.dispose && x.dispose());
            this._disposables = [];
        }
    }
    removeProperties() {
        Object.keys(this).forEach((propertyName) => {
            delete this[propertyName];
        });
    }
}
export class DisposableViewModel {
    constructor() {
        this._disposables = [];
    }
    disposeViewModel() {
        this._disposables?.forEach(disposable => disposable && disposable());
        this._disposables = [];
        const subscribableViewModel = this;
        if (subscribableViewModel) {
            subscribableViewModel._viewModelEvents?.dispose();
        }
    }
    disposeSubscription(subscription) {
        if (!subscription)
            return;
        const index = this._disposables.indexOf(subscription);
        if (index !== -1) {
            this._disposables.splice(index, 1);
            subscription();
        }
    }
    addDisposable(disposable) {
        if (disposable)
            this._disposables.push(disposable);
    }
}
